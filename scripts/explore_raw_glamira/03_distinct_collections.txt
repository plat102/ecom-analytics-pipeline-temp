use countly;

// Get distinct collection types
db.summary.distinct("collection");

// Count documents by collection type
db.summary.aggregate([
  {
    $group: {
      _id: "$collection",
      count: { $sum: 1 }
    }
  },
  { $sort: { count: -1 } }
]);
